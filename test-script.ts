import prisma from './lib/prisma'; async function main() { try { const tickets = await prisma.supportTicket.findMany({ include: { messages: { orderBy: { createdAt: 'asc' } }, user: { select: { name: true, email: true, phoneNumber: true, image: true } }, order: { select: { id: true, publicCode: true, totalAmount: true, status: true, createdAt: true, orderItems: { select: { id: true, quantity: true, unitPrice: true, menuItem: { select: { name: true } } } } } } }, orderBy: { updatedAt: 'desc' } }); console.log('Success:', tickets.length); } catch (e) { console.error('Error:', e); } finally { await prisma.$disconnect(); } } main();
