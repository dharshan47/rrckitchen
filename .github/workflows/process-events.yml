name: Process Order Events
on:
  schedule:
    - cron: "*/2 * * * *"
  workflow_dispatch:

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - run: 'curl -f "${{ secrets.APP_URL }}/api/cron/process-order-events" -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"'
